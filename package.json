{
  "name": "module-federation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean",
    "build": "lerna run --scope={loader,components-library} build",
    "example:react": "lerna run --scope components-library --scope react-example --parallel start",
    "example:vanilla": "node ./scripts/vanilla.js",
    "example:loader": "node ./scripts/react-loader.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.3.0",
    "copy-webpack-plugin": "^7.0.0",
    "copyfiles": "^2.4.1",
    "http-server": "^0.12.3",
    "lerna": "^3.22.1",
    "wait-on": "^5.2.1"
  }
}
